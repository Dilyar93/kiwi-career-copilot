# V2 开发基线

记录时间：2026-09-04（Pacific/Auckland）

开始 V2 改动前的 V1 基线：

| Check | Result |
|---|---|
| `corepack pnpm typecheck` | passed |
| `corepack pnpm lint` | passed |
| `corepack pnpm test` | 9 files, 47 tests passed |
| `corepack pnpm build` | Chrome MV3 build passed, 2.05 MB |
| `corepack pnpm test:e2e` | 4 Playwright tests passed |

基线 Chrome build 含 SEEK 和 Trade Me content scripts。V2 已确认改为 SEEK-only，删除 Trade Me 后测试数量下降不代表 SEEK 覆盖下降。

Day 1 使用公开 SEEK job-detail 页面检查了当前 DOM，确认存在以下稳定语义属性：

- `data-automation="jobDetailsPage"`
- `data-automation="job-detail-title"`
- `data-automation="advertiser-name"`
- `data-automation="job-detail-location"`
- `data-automation="job-detail-work-type"`
- `data-automation="jobAdDetails"`
- `data-automation="job-detail-apply"`

仓库中的 fixture 已匿名化，不保存原始页面、cookie、账户数据或跟踪参数。真实网站 Gate A 仍需在扩展加载后手工验证。
